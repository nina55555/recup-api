# Responsive Popup Texbox - Plan d'exécution

## ✅ Terminé

## 🔲 À faire

### 1. `src/css/Home.css` — `.welcometextt h1::before`
- Remplacer `top: 370px` (desktop, ~ligne 125) par positionnement relatif
- Remplacer `top: 330px` (mobile, ~ligne 302) par positionnement relatif

### 2. `src/css/Home.css` — `.welcometextt p`
- Remplacer `height: 22%` (desktop & mobile) par `height: auto` + `min-height`

### 3. `src/css/Home.css` — `.textbox` overflow
- Ajouter `overflow-y: auto` et padding ajusté pour mobile

### 4. `src/css/Home.css` — Nouvelle media query 320px–400px
- Ajuster padding, tailles de texte pour le `.textbox`, `.welcometextt`

